// js/views/mapView.js
// Placeholder antigo mantido para compatibilidade. A lógica real está em monitoringView.
const MapView = {
    render: async () => {
        console.warn('MapView legacy stub chamado. Utilize MonitoringView para mapa completo.');
    },
    cleanup: () => {}
};
